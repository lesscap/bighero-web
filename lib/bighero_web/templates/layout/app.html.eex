<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= assigns[:page_title] || "BigHero" %></title>
    <% assets = assigns[:assets] %>
    <%= if assets do %>
    <link rel="stylesheet" href="<%= Routes.static_url(@conn, "/css/#{assets}.css") %>">
    <% end %>
  </head>
  <body>
    <%= render @view_module, @view_template, assigns %>
    <%= if assets do %>
    <script type="text/javascript" src="<%= Routes.static_url(@conn, "/js/#{assets}.js") %>"></script>
    <% end %>
  </body>
</html>
